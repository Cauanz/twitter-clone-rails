<%= form_with model: tweet do |form|  %>
  <% if form.object.errors.any? %>
    <p class="error"><%= form.object.errors.full_messages.first %></p>
  <% end %>

  <%= form.label :body %>  
  <%= form.text_area :body %>

  <%= form.submit "Post", disable_with: 'Posting...' %>

<% end %>